# Changelog

本文件记录本插件的版本变更。

## 0.1.1

修掉 0.1.0 里会让**整个设置页打不开**的故障；除该修复外没有行为变化。

- **修复：设置页不再报 `registration.schema.toJSON is not a function`。** 宿主回答设置页时会遍历每个已注册的命名空间并调用 `schema.toJSON()`（`@deepseek-ai/dsh-settings` 的 `SettingsProvider.describe()`），而本插件注册的 `Config` 是一个带 Standard Schema 的普通函数、没有 `toJSON`，于是这次调用直接抛出。`describe()` 里没有按命名空间兜底的保护，**挂掉的是整条 `settings/describe`，不只是本插件的配置项**——同一部署下其它插件的设置卡片也一起拿不到。现在 `Config.toJSON()` 会产出设置页要的序列化 schema（Schemastery 的 `uid` + `refs` 线格式：每个节点按 id 登记，子节点用 id 引用），仍然不引入任何运行时依赖。
- **回归防线**：`test/service.test.ts` 断言序列化出来的 schema 覆盖全部六个配置键、每个字段的类型/默认值/下限，并逐条解析 `refs` 引用（引用缺失即失败）；`test/runtime.test.ts` 的假宿主改成与真宿主一样在读取时调用 `schema.toJSON()`——把修复去掉，这条测试会原样报出用户看到的那句错。
- **顺带**：`launchIntervalSeconds`（2）与 `executionTimeoutSeconds`（60）的下限提为具名常量，校验器与 schema 展示的下限从此不可能各说各话。

## 0.1.0

首个公开版本：收件箱面板与错峰调度之外，还包含「已有会话也能错峰」的定向投递、折叠历史与面板搜索。

- **历史折叠**：面板把 `已完成 / 已取消` 的旧条目收进一条默认折叠的「历史记录（N 条）」横条，`等待错峰 / 执行中 / 失败` 常驻可见；展开后一次最多画 20 条，再往下靠「再看更早的」按页追加。折叠状态记在浏览器的 `dsh.offpeak.historyOpen` 里。**一条都没删**，数据仍完整留在 `inbox.json`。
- **面板搜索**：历史横条上方新增搜索框，按标题、正文、报错、会话 id、状态、捕获日期、以及等待条目印出的「计划 … 起可执行」时刻检索（日期按面板显示时区算）；**被折叠的历史同样搜得到**，命中即列出并同样按 20 条一页翻，清空即回到原视图。
- **列表刷新更安静**：只在条目或视图真的变化时才重画列表，并保留原来的滚动位置（此前 5 秒一次的轮询会把读到一半的列表弹回顶部）。
- **热重载安全**：面板骨架带 `PANEL_VERSION`，旧版本构建的树被整体丢弃重建（旧版本存的是面板对象本身，两种布局都读）；侧边栏入口与「错峰」按钮由 `window.__dshOffpeakEpoch` 世代仲裁，并把上一代半区留下的节点**隐藏而不是删除**——删除会被旧半区的 MutationObserver 立刻插回来，两边会无限互抢同一个位置。挂载与观察器改由 `ctx.effect` 注册，正常热重载时上一代会先被卸载。
- **测试**：新增 `test/client.test.ts`，把 `lib/client.js` 当浏览器半区加载，断言折叠分组、搜索范围（含计划时刻与时区）、分页上限、世代仲裁，以及「旧半区节点被隐藏而非删除」。
- **已有会话也能错峰**：对话输入框旁新增「错峰」按钮，把写好的内容攒下来，到平价窗口再发进**当前这个会话**（高峰时段不会提前发；在平价窗口内点则立即发）。目的地取自浏览器自己持久化的当前会话，不靠猜。
- **面板可指定会话**：收件箱面板的记录区多一个「发到」下拉，可选「新会话」或任一已有会话（子代理会话不列出）；选中已有会话时按钮变为「攒到该会话」。
- **定向投递的结算**：发往已有会话的条目在投递被接受后立即记为**已完成**（附「已发到已有会话」与投递时刻）。目的会话在捕获之后仍会跑用户自己的轮次，因此不再盯着它的运行状态；投递被拒则记为**失败**并可重试。
- **「立即执行」对定向条目同样守约**：立刻投递到指定会话，不再新建会话（此前会走新建路径，与行内标注的目的地矛盾）。
- **投递失败说人话**：会话已被删除、会话当前没有可用模型这两种可处置的拒绝，在条目上直接说明；其余保留 RPC 原文便于排查。**修正**：判定此前只在 RPC 文本里找 `session/not-found` 这类标记，而宿主真实拒绝的 `code` 在 Error 属性上、消息里只有 `session "..." not found`，于是这两种拒绝都退化成了原文。现在读 `code`（并保留文本兜底），并把 `errorCode` 落到条目上。
- **提示改为按「要不要等」二选一**：窗口内点「错峰」时，`defer` 回报的 `deliverAt` 是**该窗口的起点**（一个已经过去的时刻），旧客户端按 `peak` 分支渲染，正好不显示它，所以旧提示没有说错——但判据依赖的是"当前是否高峰"而不是"这条到底要不要等"，两者在窗口边界附近会分叉。现在 `defer` 同时回报 `deliverAt` 与 `deferredWait = runAfter > now`，文案按后者二选一，不再间接推断。
- **按钮落位可诊断**：浏览器半区把输入框按钮的落位结果上报给宿主，`state.view.buttonMounted` / `buttonReason` 区分「脚本没跑」与「没找到输入框」；发送键的定位也改成"取第一个含 button 的 trailing 组"，避免斜杠菜单展开时按钮消失。
- **多标签页保护**：`dsh.sessions.current` 是同源唯一键，另一个标签页切换会话会让本标签页读到别的会话。投递前核对这个 id 是否在宿主的会话列表里，不在就不发（文案也不再谎称"稍等两秒再点一次"——列表里确实没有这个会话时，重试永远不会成功）；提示文案里写出目的会话标题。
- **接口**：新增同源 `GET /api/offpeak-inbox/sessions`，`action` 新增 `defer` 与 `buttonState`。

- **收件箱面板**：侧边栏入口，随时记录标题 + 正文，条目持久化到 `$DSH_HOME/offpeak-inbox/inbox.json`。
- **错峰调度**：按 DeepSeek 官方峰谷口径（工作日 01:00–04:00、06:00–10:00 UTC 为高峰）在平价窗口内自动新建会话执行；计划时刻在捕获当下即确定，因此错过的窗口不补跑、条目也不会丢。
- **状态与操作**：等待错峰 / 执行中 / 已完成 / 失败；支持编辑、删除、立即执行、失败重试。
- **投递方式**：条目一律经 `session/prompt` 交给宿主组装会话消息（插件不自行拼装消息结构），运行中的 agent 句柄只用于读运行状态。
- **执行结算**：进程内 agent 状态与会话名册双通道判定；名册读取失败时视为未知，绝不误判完成。宿主上报的轮次失败（`api-session/error`）会把条目标成**失败**并保留原因，不再显示成"已完成"。
- **健壮性**：执行超时保护、原子落盘（临时文件 + 改名）、损坏账本改名保留现场并报错。
- **接口**：同源回环 HTTP 接口（state / action / events），带方法、内容类型与体积校验。
